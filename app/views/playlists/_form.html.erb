<%= form_for @playlist do |f| %>
    <%= f.label :title %>
    <%= f.text_field :title %>
    <%= f.text_field :user_id, :value => "#{@current_user.id}", :type => "hidden"%>
    <%= f.submit %>
<%end%>